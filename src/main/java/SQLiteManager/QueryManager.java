package SQLiteManager;

import java.util.List;

public class QueryManager {

    /*
     * I thought it would make sense to include all queries here.
     * However, then all parameters (active columns, etc.) would have passed
     * to the functions which decreases readability.
     *
     * I think it's better to keep queries in the respective controller classes (where they are needed).
     */

}
